/*const {createLogger,transports,format} = require('winston');
const winston = require("winston");
require('winston-mongodb');

//logging function 
const genrelogger = createLogger({
       winston.transports.MongoDB({
        db:'mongodb://localhost/vidly',
          level:'error'
       })
});  */